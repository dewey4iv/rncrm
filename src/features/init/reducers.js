import { createSlice } from '@reduxjs/toolkit'

const name = 'init'

const initialState = {
}

const reducers = {
}

const slice = createSlice({
    name,
    initialState,
    reducers,
})

export const {
} = slice.actions

export default slice.reducer
